---
description: Run Android/KMP unit tests, convert to Allure format, generate combined report (Android + backend), open in browser at http://127.0.0.1:4040
---

Use the android-tests agent to run the Kotlin Multiplatform unit tests for this project, convert the JUnit XML results to Allure format, merge them with any existing backend test results, generate a combined Allure HTML report, and open it in the browser.
