# HTTP client for API testing
httpx>=0.27.0

# Async HTTP test support
pytest-asyncio>=0.23.0

# Pytest core + plugins
pytest>=8.0.0
pytest-cov>=5.0.0
pytest-mock>=3.12.0
pytest-xdist>=3.5.0          # parallel test execution
pytest-timeout>=2.3.0

# HTTP mocking (for unit tests — no live server required)
respx>=0.21.0

# Fake data generation (Factory pattern)
faker>=24.0.0
factory-boy>=3.3.0

# Schema / contract validation
pydantic>=2.6.0
jsonschema>=4.21.0

# Allure reporting
allure-pytest>=2.13.5

# Utilities
python-dotenv>=1.0.0
